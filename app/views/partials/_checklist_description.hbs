<div class='checklist-description-section'>
  <div class='content-width content-inset'>
    <div class='checklist-description editable-body'>
      {{description}}
      {{#if isEditing}}
        {{view App.TextArea valueBinding='description'}}
      {{/if}}
    </div>
  </div>
</div>
